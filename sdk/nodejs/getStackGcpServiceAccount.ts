// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * > **Note:** `spacelift.StackGcpServiceAccount` is deprecated. Please use `spacelift.GcpServiceAccount` instead. The functionality is identical.
 *
 * `spacelift.StackGcpServiceAccount` represents a Google Cloud Platform service account that's linked to a particular Stack or Module. These accounts are created by Spacelift on per-stack basis, and can be added as members to as many organizations and projects as needed. During a Run or a Task, temporary credentials for those service accounts are injected into the environment, which allows credential-less GCP Terraform provider setup.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as spacelift from "@spacelift-io/pulumi-spacelift";
 *
 * // For a Module
 * const k8s_module = spacelift.getStackGcpServiceAccount({
 *     moduleId: "k8s-module",
 * });
 * // For a Stack
 * const k8s_core = spacelift.getStackGcpServiceAccount({
 *     stackId: "k8s-core",
 * });
 * ```
 */
export function getStackGcpServiceAccount(args?: GetStackGcpServiceAccountArgs, opts?: pulumi.InvokeOptions): Promise<GetStackGcpServiceAccountResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("spacelift:index/getStackGcpServiceAccount:getStackGcpServiceAccount", {
        "moduleId": args.moduleId,
        "stackId": args.stackId,
    }, opts);
}

/**
 * A collection of arguments for invoking getStackGcpServiceAccount.
 */
export interface GetStackGcpServiceAccountArgs {
    /**
     * ID of the stack which uses GCP service account credentials
     */
    moduleId?: string;
    /**
     * ID of the stack which uses GCP service account credentials
     */
    stackId?: string;
}

/**
 * A collection of values returned by getStackGcpServiceAccount.
 */
export interface GetStackGcpServiceAccountResult {
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * ID of the stack which uses GCP service account credentials
     */
    readonly moduleId?: string;
    /**
     * email address of the GCP service account dedicated for this stack
     */
    readonly serviceAccountEmail: string;
    /**
     * ID of the stack which uses GCP service account credentials
     */
    readonly stackId?: string;
    /**
     * list of Google API scopes
     */
    readonly tokenScopes: string[];
}
/**
 * > **Note:** `spacelift.StackGcpServiceAccount` is deprecated. Please use `spacelift.GcpServiceAccount` instead. The functionality is identical.
 *
 * `spacelift.StackGcpServiceAccount` represents a Google Cloud Platform service account that's linked to a particular Stack or Module. These accounts are created by Spacelift on per-stack basis, and can be added as members to as many organizations and projects as needed. During a Run or a Task, temporary credentials for those service accounts are injected into the environment, which allows credential-less GCP Terraform provider setup.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as spacelift from "@spacelift-io/pulumi-spacelift";
 *
 * // For a Module
 * const k8s_module = spacelift.getStackGcpServiceAccount({
 *     moduleId: "k8s-module",
 * });
 * // For a Stack
 * const k8s_core = spacelift.getStackGcpServiceAccount({
 *     stackId: "k8s-core",
 * });
 * ```
 */
export function getStackGcpServiceAccountOutput(args?: GetStackGcpServiceAccountOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetStackGcpServiceAccountResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("spacelift:index/getStackGcpServiceAccount:getStackGcpServiceAccount", {
        "moduleId": args.moduleId,
        "stackId": args.stackId,
    }, opts);
}

/**
 * A collection of arguments for invoking getStackGcpServiceAccount.
 */
export interface GetStackGcpServiceAccountOutputArgs {
    /**
     * ID of the stack which uses GCP service account credentials
     */
    moduleId?: pulumi.Input<string>;
    /**
     * ID of the stack which uses GCP service account credentials
     */
    stackId?: pulumi.Input<string>;
}
